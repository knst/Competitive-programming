#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main()
{
    int n;
    cin >> n;
    vector<int> v[i];
    for (int i = 0; i < n; ++i)
        cin >> v[i];
    random_shuffle(v.begin(), v.end());


    long long game = 0;
    long long judge = 0;
    for (int i = 0; i < n; ++i) {
        if (v[0] > judge) {
            judge +=



    return 0;
}
