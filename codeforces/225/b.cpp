#include <iostream>
#include <vector>
#include <set>
#include <algorithm>
#include <map>
#include <queue>

using namespace std;

int main() {
    int n;
    cin >> n;
    int m;
    cin >> m;
    vector<pair<int,int>> v;
    for (int i = 0; i < m; ++i) {
        int x;
        int y;
        cin >> x >> y;
        v.push_back(make_pair(x, y));
    }





    return 0;
}
